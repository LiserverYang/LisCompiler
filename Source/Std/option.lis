// Copyright 2026, LiserverYang. All rights reserved.
// MIT License.
//
// option.lis
//
// A minimal optional-value type. `value` is only meaningful when `is_some`
// is true; None is conventionally represented with a zeroed payload.

struct Option<T>
{
    pub is_some: bool,
    pub value: T
}

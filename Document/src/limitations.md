# 已知限制与路线图

<!-- grammar_name: limitations, search_name: 已知限制,路线图,未实现 -->

本页汇总语言当前未实现/待定的部分。标注「规范决策」的语义已定案；
标注「待定」的取舍尚未定案。

## 已定案的规范决策（2026-08-12）

| 决策 | 内容 |
|---|---|
| `let x;` 无初始化 | 计划**编译期拒绝读取未初始化变量**（当前读取是 UB，未实现） |
| 默认参数 `fn f(a: i32 = 5)` | **移除**：死语法（解析后全阶段忽略），现在直接报错 |
| `#[i_know]` 属性 | **已实现**：语句前属性，窄化 cast 错误降级为警告 |
| `move` 关键字 | 设计语义：**每次使用时在使用处移动构建**（类似 Rust 相关语法）；**未实现**，当前 `let move` 无效果 |

## 未实现的语言特性

### 语法面

- **一元运算符**：`-`（取反）、`!`（逻辑非）、`~`（按位非）——设计语法，无实现；
  负数写作 `0 - x`。
- **`^` `<<` `>>` 运算符**：无 token；仅方法 `bitxor`/`shl`/`shr` 可调用。
- **模块限定的关键字名**：`char::is_digit`（模块名是类型关键字）在表达式中无法
  模块限定访问（`char` 词法为 CHAR token 而非 IDENTIFIER）；用选择性导入
  `impt char { is_digit };` 的裸名访问。`impt char;` 本身合法。
- **函数类型语法**：函数指针存在（间接调用可用），但无类型语法。
- **复合赋值**：无 `+=` 等，写作 `s = s + x`。
- **`if` 表达式**：if 是语句；值产生用 match 表达式。
- **属性**：仅 `#[i_know]` 一种；无其它属性语法。
- **闭包**、**解引用运算符** `*x`、**字符串拼接**：无。

### 语义面

- **panic / never 类型**：无；`unwrap`/`expect` 不可实现（`unwrap_or` 是安全替代）。
  数组越界用 libc `abort()`。
- **引用模式匹配**（`match &opt`）：未实现。
- **返回借用追踪**：`to_cstr` 返回的借用编译器不追踪，调用方负责 owner 存活。
- **未初始化读取拒绝**（见上，规范决策待实现）。
- **`let x = Option::None` 无上下文推断**：失败，需期望类型。
- **嵌套模式 / or 模式 / 字面量模式**：match 模式仅 `_`/单元变体/载荷绑定。
- **数组元素借用精度**：`a[i]` 用通配路径 `[*]`，不相交元素借用被保守拒绝。
- **数组**：元素仅 Copy、禁引用、禁作参数/返回（用 `&[T; N]`）、尺寸 ≤ 1<<20。
- **运行时条件 drop**（MIR edge-splitting）：未实现。
- **`if` 作表达式时的崩溃**（`let x = if c { 1 } else { 0 };`）：已知崩溃，勿用。

### 语义取舍（保守关闭，防误报）

- 悬垂返回检查对无法判定来源的引用（Unknown）放行（漏报不误报）。
- 借用检查对复杂索引/解引用链保守（见[借用检查](./borrow.md)的已知边界）。
- OOM 不检查（`malloc` 失败即运行时崩溃）。

## 路线图

按可用性优先级：

1. **panic/never 类型**：解锁 `unwrap`/`expect`/`Result`。
2. **编译期拒绝未初始化读取**（规范决策落地）。
3. **`move` 语义实现**（用户定义：使用处移动构建）。
4. **Vec/堆集合**：基于 String 堆机制扩展；非 Copy 数组元素。
5. **数组精确索引路径**：允许不相交元素借用。
6. **extern/FFI 机制**：声明外部函数（codegen 已就绪）。
7. **String 增强**：格式化/拼接。
8. **引用模式 D4**：`match &opt`。
9. **一元运算符**（`-`/`!`/`~` 与 `Neg`/`Not` trait）、复合赋值。

每项实现前如有语义取舍，由语言设计者（用户）拍板后更新本手册。
